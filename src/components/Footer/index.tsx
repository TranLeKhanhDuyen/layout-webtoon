import { Box } from "theme/base";

function Footer() {
  return (
    <Box
      sx={{
        display: "flex",
        alignItems: "center",
        justifyContent: "space-between",
      }}
    >
      FOOTER
    </Box>
  );
}

export default Footer;
